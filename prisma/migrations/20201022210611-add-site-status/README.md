# Migration `20201022210611-add-site-status`

This migration has been generated by Andrew Fulton at 10/22/2020, 4:06:12 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."WebsiteStatus" AS ENUM ('PRIVATE', 'PUBLISHED')

DROP INDEX "public"."website.name_unique"

ALTER TABLE "public"."website" DROP COLUMN "name",
ADD COLUMN "title" text   NOT NULL ,
ADD COLUMN "location" text   NOT NULL ,
ADD COLUMN "status" "WebsiteStatus"  NOT NULL DEFAULT E'PRIVATE'

CREATE UNIQUE INDEX "website.title_unique" ON "public"."website"("title")

CREATE UNIQUE INDEX "website.location_unique" ON "public"."website"("location")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201020173149-website-name-unique..20201022210611-add-site-status
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -15,8 +15,13 @@
   ADMIN
   DEVELOPER
 }
+enum WebsiteStatus {
+  PRIVATE
+  PUBLISHED
+}
+
 model Account {
   id                 Int       @default(autoincrement()) @id
   compoundId         String    @unique @map(name: "compound_id")
   userId             Int       @map(name: "user_id")
@@ -77,9 +82,11 @@
 model Website {
   id            Int     @default(autoincrement()) @id
   userId        Int     @map(name: "user_id")
   user          User    @relation(fields: [userId], references: [id])
-  name          String  @unique
+  title         String  @unique
+  location      String  @unique
+  status        WebsiteStatus @default(PRIVATE)
   pages         Page[]
   defaultTheme  String  @map("default_theme")
   createdAt  DateTime   @default(now()) @map(name: "created_at")
   updatedAt  DateTime   @default(now()) @map(name: "updated_at")
```


