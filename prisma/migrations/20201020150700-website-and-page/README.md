# Migration `20201020150700-website-and-page`

This migration has been generated by Andrew Fulton at 10/20/2020, 10:07:00 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "public"."Role" AS ENUM ('USER', 'ADMIN', 'DEVELOPER')

ALTER TABLE "public"."users" ADD COLUMN "role" "Role"  NOT NULL DEFAULT E'USER'

CREATE TABLE "public"."website" (
"id" SERIAL,
"user_id" integer   NOT NULL ,
"default_theme" text   NOT NULL ,
"created_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updated_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."page" (
"id" SERIAL,
"path" text   NOT NULL ,
"blocks" jsonb   NOT NULL ,
"created_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updated_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"websiteId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "website_user_id_unique" ON "public"."website"("user_id")

ALTER TABLE "public"."website" ADD FOREIGN KEY ("user_id")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."page" ADD FOREIGN KEY ("websiteId")REFERENCES "public"."website"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201014193254-next-auth-models..20201020150700-website-and-page
--- datamodel.dml
+++ datamodel.dml
@@ -2,15 +2,21 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
+enum Role {
+  USER
+  ADMIN
+  DEVELOPER
+}
+
 model Account {
   id                 Int       @default(autoincrement()) @id
   compoundId         String    @unique @map(name: "compound_id")
   userId             Int       @map(name: "user_id")
@@ -43,15 +49,18 @@
 }
 model User {
   id            Int       @default(autoincrement()) @id
+  role          Role      @default(USER)
   name          String?
   email         String?   @unique
   emailVerified DateTime? @map(name: "email_verified")
   image         String?
   createdAt     DateTime  @default(now()) @map(name: "created_at")
   updatedAt     DateTime  @default(now()) @map(name: "updated_at")
+  website       Website?
+
   @@map(name: "users")
 }
 model VerificationRequest {
@@ -63,4 +72,27 @@
   updatedAt  DateTime @default(now()) @map(name: "updated_at")
   @@map(name: "verification_requests")
 }
+
+model Website {
+  id            Int   @default(autoincrement()) @id
+  userId        Int   @map(name: "user_id")
+  user          User  @relation(fields: [userId], references: [id])
+  pages         Page[]
+  defaultTheme  String  @map("default_theme")
+  createdAt  DateTime @default(now()) @map(name: "created_at")
+  updatedAt  DateTime @default(now()) @map(name: "updated_at")
+
+  @@map(name: "website")
+}
+
+model Page {
+  id         Int      @default(autoincrement()) @id
+  path       String
+  website    Website
+  blocks     Json
+  createdAt  DateTime @default(now()) @map(name: "created_at")
+  updatedAt  DateTime @default(now()) @map(name: "updated_at")
+
+  @@map(name: "page")
+}
```
